import { beforeEach,describe,expect,it } from "vitest";import { createQuoteDraft } from "@/lib/quote-request/defaults";import { QUOTE_DRAFT_KEY } from "@/lib/quote-request/constants";import { clearQuoteDraft,loadQuoteDraft,saveQuoteDraft } from "@/lib/quote-request/storage";
describe("quote request draft",()=>{beforeEach(()=>localStorage.clear());it("saves non-sensitive answers and restores them",()=>{const d=createQuoteDraft();d.project.title="Cuisine";d.trades.primary="kitchen";saveQuoteDraft(d);expect(loadQuoteDraft()?.data.project.title).toBe("Cuisine");expect(loadQuoteDraft()?.data.trades.primary).toBe("kitchen");});it("excludes contact, exact address, coordinates and files",()=>{const d=createQuoteDraft();d.contact.email="private@example.com";d.contact.phone="0600000000";d.location.address="1 private street";d.location.coordinates="1,2";saveQuoteDraft(d);const raw=localStorage.getItem(`${QUOTE_DRAFT_KEY}_MA`)??"";for(const secret of ["private@example.com","0600000000","1 private street","1,2","File"])expect(raw).not.toContain(secret);});it("expires and deletes stale drafts",()=>{saveQuoteDraft(createQuoteDraft(),new Date("2026-01-01"));expect(loadQuoteDraft("MA",new Date("2026-03-01"))).toBeNull();});it("clears on request",()=>{saveQuoteDraft(createQuoteDraft());clearQuoteDraft();expect(loadQuoteDraft()).toBeNull();});});
