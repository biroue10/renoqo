import { QUOTE_DRAFT_KEY, QUOTE_DRAFT_MAX_AGE_MS, QUOTE_DRAFT_VERSION } from "./constants";
import type { QuoteDraft, QuoteDraftEnvelope } from "./types";
export function saveQuoteDraft(data:QuoteDraft,now=new Date()):QuoteDraftEnvelope|null{const safe={...data,contact:{...data.contact,firstName:"",lastName:"",email:"",phone:"",whatsapp:"",organization:""},location:{...data.location,address:"",coordinates:"",onsitePerson:""}};const envelope={version:QUOTE_DRAFT_VERSION,country:data.location.country,savedAt:now.toISOString(),expiresAt:new Date(now.getTime()+QUOTE_DRAFT_MAX_AGE_MS).toISOString(),data:safe};try{localStorage.setItem(`${QUOTE_DRAFT_KEY}_${data.location.country}`,JSON.stringify(envelope));return envelope;}catch{return null;}}
export function loadQuoteDraft(country="MA",now=new Date()):QuoteDraftEnvelope|null{try{const key=`${QUOTE_DRAFT_KEY}_${country}`;const raw=localStorage.getItem(key);if(!raw)return null;const value=JSON.parse(raw) as QuoteDraftEnvelope;if(value.version!==QUOTE_DRAFT_VERSION||Date.parse(value.expiresAt)<=now.getTime()){localStorage.removeItem(key);return null;}return value;}catch{return null;}}
export function clearQuoteDraft(country="MA"){try{localStorage.removeItem(`${QUOTE_DRAFT_KEY}_${country}`);}catch{}}
