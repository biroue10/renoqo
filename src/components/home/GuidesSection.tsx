import Link from "next/link";
import { Container } from "@/components/ui/Container";
import { SectionHeading } from "@/components/ui/SectionHeading";
import { guides } from "@/data/guides";
export function GuidesSection() { return <section className="section section-tint"><Container><div className="section-split"><SectionHeading align="left" eyebrow="Conseils et inspiration" title="Mieux préparer votre projet" description="Des guides concrets pour budgéter, comparer et avancer avec davantage de sérénité." /><Link href="/guides" className="text-link">Tous les guides →</Link></div><div className="guide-grid">{guides.map((guide, i) => <article className="guide-card" key={guide.slug}><div className={`guide-image ${guide.tone}`} role="img" aria-label={`Illustration abstraite : ${guide.title}`}><span>{String(i + 1).padStart(2, "0")}</span><i /><i /></div><div className="guide-content"><div className="guide-meta"><span>{guide.category}</span><span>{guide.readTime}</span></div><h3><Link href={`/guides/${guide.slug}`}>{guide.title}</Link></h3><p>{guide.summary}</p><Link href={`/guides/${guide.slug}`} className="card-link">Lire le guide →</Link></div></article>)}</div></Container></section>; }
